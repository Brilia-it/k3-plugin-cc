interface:
  display_name: "Kimi Setup"
  short_description: "Check hooks, models and provider setup"
  default_prompt: "Use $k3-setup to check the local Kimi companion setup."
policy:
  allow_implicit_invocation: false
